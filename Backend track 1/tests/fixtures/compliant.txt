# Compliant Fixture
authentication.mfa_enabled = true
authentication.password_min_length = 14
authentication.password_complexity_enabled = yes
authentication.lockout_threshold = 5

access.default_admin_disabled = true
network.ssh.enabled = on
network.insecure_management_port.enabled = false

logging.audit.enabled = true
logging.remote_logging.enabled = true
time.ntp.enabled = true
time.ntp.servers = 10.0.0.1, 10.0.0.2

# This will trigger a sensitive redaction but should not fail
authentication.password = SuperSecret123!
