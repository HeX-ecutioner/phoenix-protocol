# Phoenix Protocol — Frontend Environment Configuration
# Authoritative backend API base URL (defaults to http://localhost:5000)
VITE_API_BASE_URL=http://localhost:5000

# Set to true only for isolated frontend development with mock data.
# When omitted or false, the frontend requires the live Flask backend.
VITE_USE_MOCK=false
